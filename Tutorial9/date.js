//numbers include  to  power of 53 
let car={
    make:"Toyota",
    year:2024,
    model: 'corolla'
}
// getCarInfo: function() {
//     return `Make: ${this.make}, Model: ${this.model}, Year: ${this.year}`;
// }
//  car.getCarInfo = function(car) { `return Make: ${this.make} Model: ${this.model} Year: ${this.year}`;
//  };
console.log(car);
let i = 10;
while (i > 0) {
    console.log(i--); // Outputs 5, 4, 3, 2, 1
}
console.log(i); // Outputs 0 after the loop ends